# Cursor next phase — first production unit atlas

The deterministic atlas animation runtime is implemented. The next recommended
vertical slice is one complete Human Swordsman atlas with `idle`, `walk`,
`attack`, `hit`, and `death` in `NE`, `SE`, `SW`, and `NW`, plus its team-color
mask and Manifest v2 metadata. Do not expand to every unit until this one atlas
passes pivot, silhouette, frame-timing, fallback and in-game scale review.
